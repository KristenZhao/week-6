/* ================================
Week 6 Assignment: Midterm Functions + Signatures
================================ */
// color by illumination function
var colorFeature = function(illumination){
  ???
};

var filterBySeason = function(season){
  // if crash_month === 12, 1, 2, then === winter
  // if crash_month === 3, 4, 5, then === spring
  // etc.
}
